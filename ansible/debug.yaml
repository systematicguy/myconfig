- name: debug
  hosts: localhost
  gather_facts: true
  vars_files:
    - vars/wsl.yaml
    - vars/user_config.yaml
  vars:
    # skip_package_install: true
  roles:
    #- role: tools/terraform
    - role: tools/go
  tasks:
