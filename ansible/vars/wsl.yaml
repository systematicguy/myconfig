ansible_become_password: "{{ lookup('env', 'ANSIBLE_BECOME_PASSWORD') }}"
package_installer_role: helpers/apt  # TODO evaluate https://docs.ansible.com/ansible/latest/collections/ansible/builtin/package_module.html
bin_dir: "~/.local/bin"